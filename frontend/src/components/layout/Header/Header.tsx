import React from "react";

const Header: React.FC = () => (
  <header className="h-20 flex items-center justify-center relative top-0 w-full">
    <h1 className="text-3xl">HEADER MUSIC</h1>
  </header>
);

export default Header;